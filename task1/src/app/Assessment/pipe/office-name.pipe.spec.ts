import { OfficeNamePipe } from './office-name.pipe';

describe('OfficeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new OfficeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
